const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const props = await prisma.property.findMany({ select: { id: true, parent_id: true, status: true, title_en: true } }); console.log(JSON.stringify(props, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
